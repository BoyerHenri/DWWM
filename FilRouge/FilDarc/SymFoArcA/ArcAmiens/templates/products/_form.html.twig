{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

 {# Reprend l'éléments ProductName #}
<div class="form-row">

    <div class="form-group col-md-6">
        {{ form_row(form.ProductName) }}
    </div>

 {# Reprend l'éléments SupplierID #}
    <div class="form-group col-md-6">
        {{ form_row(form.SupplierID) }}
      </div>

 {# Reprend l'éléments CategoryID #}
    <div class="form-group col-md-4">
        {{ form_row(form.CategoryID) }}
    </div>

 {# Reprend l'éléments Quantité Par Unité #}
    <div class="form-group col-md-4">
        {{ form_row(form.QuantityPerUnit) }}
    </div>

 {# Reprend l'éléments  Prix Unitaire#}
    <div class="form-group col-md-4">
        {{ form_row(form.UnitPrice) }}
    </div>

 {# Reprend l'éléments Quantité En Stock #}
    <div class="form-group col-md-4">
        {{ form_row(form.UnitsInStock) }}
    </div>

 {# Reprend l'éléments Quantité En Commande #}
    <div class="form-group col-md-4">
        {{ form_row(form.UnitsOnOrder) }}
    </div>

 {# Reprend l'éléments Niveau D'alerte#}   
    <div class="form-group col-md-4">
        {{ form_row(form.ReorderLevel) }}
    </div>

 {# Reprend l'éléments Photo#} 
    <div class="form-group col-md-6">
        {{ form_row(form.picture2) }}
    </div>


 {# Reprend l'éléments Discontinued#} 
    <div class="form-group col-md-6">
        {{ form_row(form.Discontinued) }}
    </div>

</div>

<button class="btn btn-primary float-right m-2">{{ button_label|default('Sauvegarder') }}</button> {# Correspond Au Bouton Validation #}

{{ form_end(form) }}
